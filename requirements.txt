# SEC 10-K Research Advisor — Streamlit Cloud requirements
# ---------------------------------------------------------------
# Core ML / retrieval
sentence-transformers>=2.7.0
numpy>=1.26.0
rank-bm25>=0.2.2

# LangChain core + cloud-compatible LLM backend
langchain-core>=0.2.0
langchain-openai>=0.1.0      # uses OPENAI_API_KEY from Streamlit secrets

# Optional: swap the line above for either of these if preferred
# langchain-anthropic>=0.1.0  # uses ANTHROPIC_API_KEY from Streamlit secrets

# NOTE: faiss-cpu intentionally omitted.
# faiss-cpu 1.13.x installs on Python 3.14 but fails to import at runtime
# due to a known ABI incompatibility. The agent automatically falls back to
# numpy cosine similarity search, which is functionally equivalent for this corpus.
# Re-add once faiss-cpu ships a Python 3.14-compatible wheel:
#   faiss-cpu>=1.8.0

# Streamlit (pinned to avoid surprises on Streamlit Cloud)
streamlit>=1.35.0

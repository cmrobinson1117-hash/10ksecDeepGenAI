# SEC 10-K Research Advisor — Streamlit Cloud requirements
# ---------------------------------------------------------------
# Core ML / retrieval
sentence-transformers>=2.7.0
numpy>=1.26.0
rank-bm25>=0.2.2

# LangChain core + cloud-compatible LLM backend
langchain-core>=0.2.0
langchain-openai>=0.1.0      # uses OPENAI_API_KEY from Streamlit secrets

# Optional: swap the line above for either of these if preferred
# langchain-anthropic>=0.1.0  # uses ANTHROPIC_API_KEY from Streamlit secrets

# faiss — use the CPU wheel (no CUDA required on Streamlit Cloud)
# If this still fails, delete this line; the code falls back to numpy search.
faiss-cpu>=1.7.4

# Streamlit (Streamlit Cloud installs this automatically, but pinning avoids surprises)
streamlit>=1.35.0
